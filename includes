app_name="upsdiagd"

# determine controller's identity
host_name=$(hostname)

# construct database paths
database_filename="upsdata.sqlite3"
local_db_path="${HOME}/.sqlite3"
remote_db_path="/mnt/data"
